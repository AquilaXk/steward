# Steward 0.2.0 verification

Environment: macOS arm64, Node v22.16.0, npm 10.9.2. The Node runtime matches
`.nvmrc`. Historical 0.1.1 artifacts remain unchanged in this directory.

- Run `npm test`: 151 tests pass, with no failures, skips or cancellations.
- Add failing regressions before implementing bounded current-memory queries,
  immutable schedule revisions and managed installation lifecycle operations.
- Verify that query filters cannot conceal a corrupt journal record, schedule
  forks and identity changes fail, customized files survive removal, and an
  update conflict leaves host configuration unchanged.
- Correct a Windows-only test assumption discovered in CI: compare the recorded
  CLI path with the actual platform-native toolkit path, rather than searching
  for a POSIX slash substring. The runtime path itself required no change.
- Validate the Claude plugin with Claude Code 2.1.226. Its warning identifies
  root `CLAUDE.md` as developer context, not plugin context; installed project
  setup creates the actual import. The plugin ships its procedures as skills.

The local Plugin Creator helper rejects custom skill directory names. Steward
uses the current [official manifest path contract](https://developers.openai.com/plugins/build/plugins),
which resolves bundled component paths relative to the plugin root. The repository
check validates those exact paths and generated Claude artifacts. The helper's
directory convention is not claimed as a passing native loader test.

## Native observations

The owner authorized transmitting public Steward code and synthetic fixture data
and attempting one temporary file edit per supervised smoke run. No global host
configuration or persistent native trust record was changed.

| Host | Observed result | Limit |
|---|---|---|
| Claude Code 2.1.226 | Discover all eight namespaced plugin skills; execute SessionStart and UserPromptSubmit successfully, producing two Steward receipts | The `fable` alias resolved to `claude-fable-5`; OAuth failed before model work. The owner has no Claude subscription, so no model behavior is scored |
| Codex CLI 0.153.4, `gpt-6-astra` | Follow the recall procedure and return the one current color record, excluding the expired assumption | Ephemeral `exec` runs produced no Steward hook receipts; the protected edit succeeded. These runs do not establish hook enforcement |
| Codex native app-server 0.153.4 | List all four project hooks with the expected source, commands, events and matchers, without loader warnings | Hooks remain untrusted; session overrides did not establish persisted trust. The model smoke did not proceed through this path |

One-off hook-trust bypass and an explicit hooks feature flag did not produce hook
receipts in the tested `exec` sessions. This is a recorded failed enforcement
observation, not evidence that configuration discovery guarantees execution.
Complete the actual native `/hooks` review and observe a denied harmless edit
before relying on interception in a project. `doctor` continues to report native
verification as unchecked. The installer does not bypass that boundary.

The nineteen-case behavioral rubric is not fully scored; the bounded smoke is
not a comparative benchmark or a claim of universal model superiority. The
required CI matrix passed on Linux, macOS and Windows with Node 22 and 24 at
`2bf06447a2404cf2d5100fc97f87190e3474dcbe`. Later documentation revisions use their
own current-head checks on [PR #3](https://github.com/AquilaXk/steward/pull/3).
